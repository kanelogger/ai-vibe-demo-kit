# Project Rules

根目录 `AGENTS.md` 只保留高频红线和导航。详细规则按任务加载，避免把无关上下文全部塞给 Agent。

推荐主题：

- `testing.md`：风险分层验证与报告要求。
- `security.md`：权限、敏感数据和外部系统边界。
- `git.md`：工作区保护、提交和发布规则。

项目启用时应把占位内容替换成真实命令和边界。规则冲突时保留证据，选择权威性更高且更新更近的来源，禁止自行拼接出第三套规则。
