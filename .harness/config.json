{
  "version": 1,
  "project": {
    "name": "AI Native Harness Overlay",
    "summary": "Repository-owned workflow control plane for Agent context, gates, Skill routing, verification, and recovery.",
    "hasUserInterface": false
  },
  "commands": {
    "quick": {
      "static": ["node --check scripts/harness/lib/skill-routing.mjs && node --check scripts/harness/cli.mjs && node --check scripts/harness-check.mjs"],
      "test": ["node --test scripts/harness/test/skill-routing.test.mjs"]
    },
    "full": {
      "static": ["node --check scripts/harness/lib/skill-routing.mjs && node --check scripts/harness/cli.mjs && node --check scripts/harness-check.mjs"],
      "test": ["node --test scripts/harness/test/*.test.mjs"]
    },
    "contracts": []
  },
  "criticalUserPaths": [],
  "git": {
    "targetRef": "refs/heads/main",
    "stateRef": "refs/heads/harness/state"
  },
  "verification": {
    "reportPath": ".harness/verification-report.json",
    "maxAgeHours": 24,
    "commandTimeoutMs": 600000,
    "workspaceFingerprint": "git"
  },
  "recovery": {
    "testDataCleanup": [],
    "rollback": []
  },
  "notes": "本文件保存机器需要执行的验证事实。project.name、project.summary 必填；commands.quick.static 和 commands.quick.test 至少各登记一条真实命令；full 组为空时回退到 quick。SPECS/api.md 或 SPECS/database.md 存在时，commands.contracts 必须登记契约校验命令。criticalUserPaths 的 verify 使用 { mode: command, command }，人工路径使用 { mode: manual, instructions, evidence }。recovery.testDataCleanup 使用 { mode: command, command } 或 { mode: none, reason }。verification 控制报告位置、有效期、超时和工作区指纹；workspaceFingerprint 设为 none 时必须在 notes 说明等价审计方式。"
}
