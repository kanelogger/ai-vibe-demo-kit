# Coding Agent Rules

这些规则适用于涉及代码、脚本、配置文件、构建文件、测试、CLI、自动化或开发者工作流的任务。

**权衡：** 在重要任务上，这些准则偏向谨慎而非速度；琐碎任务可自行判断。

## 规则 1 — 先思考，再编码

**不要假设。不要隐藏困惑。把取舍摆上桌面。**

开始之前：

- 明确陈述你的假设。拿不准时，问，而不是猜。
- 存在歧义时，给出多种解读。
- 有更简单的方法时，提出反驳建议。
- 困惑时就停下，说明哪里不清楚。

## 规则 2 — 简单优先

**解决当前问题所需的最少代码。不做投机性扩展。**

- 不写未要求的功能。
- 不为一次性使用场景做抽象。
- 不增加未要求的灵活性或可配置性。
- 不为不可能出现的场景写错误处理。
- 如果写了 200 行而 50 行就能解决，重写。

检验标准：资深工程师会觉得这过度复杂吗？会，就简化。

## 规则 3 — 外科手术式改动

**只动必须动的，只清理自己的烂摊子。**

修改既有代码时：

- 不要顺带"改进"无关的代码、注释或格式。
- 不要重构没有坏掉的东西。
- 匹配现有风格，即使你自己会写得不同。
- 注意到无关的死代码就提出来，不要删掉它。

改动造成孤儿时：

- 移除你的改动使不再使用的 import、变量和函数。
- 不要删除先于你的改动就存在的死代码，除非要求你删。

检验标准：改动的每一行都必须能追溯到你收到的需求。

## 规则 4 — 目标驱动执行

**定义成功标准，循环直到通过验证。**

把任务转化为可验证的目标：

- "加检查"变成——为非法输入写测试，再让它们通过。
- "修 bug"变成——写一个能复现 bug 的测试，再让它通过。
- "重构 X"变成——重构前后保证测试都通过。

多步任务先写一个简短计划：

```text
1. [步骤] -> 验证：[检查项]
2. [步骤] -> 验证：[检查项]
3. [步骤] -> 验证：[检查项]
```

强的成功标准让你能够独立循环；弱的成功标准——比如"让它正常跑起来"——需要澄清。

## 规则 5 — 模型只做判断

模型用于分类、起草、总结、提取、权衡分析和判断性工作。

不要把模型用于路由、重试、确定性转换；普通代码能可靠完成的例行工作，也不要丢给模型。

如果代码能回答，就让代码回答。

## 规则 7 — 冲突摆上桌面，不取平均

两个模式冲突时，选一个有证据支持的：近期相关性、测试用例、本地惯例、可靠性，并解释为什么选它。

把互相冲突的模式标记出来待清理。不要搅拌成第三种偶成的风格。

## 规则 8 — 先读后写

添加代码前，先读导出、调用者、共享工具、邻近测试、本地惯例。

"看起来正交"是危险信号。如果你不理解为什么代码这么写，要调查，要问，不要动手。

## 规则 9 — 测试验证意图，不只是行为

测试必须表达为什么这个行为有意义，而不只是记录今天看到什么输出。

如果业务逻辑变了一个测试不会失败，这个测试就是不对的测试。

## 规则 10 — 每完成一个重要步骤，检查点

每完成一个重要步骤，小结：

- 做了什么。
- 验证了什么。
- 还剩什么。

不要从你无法回溯描述的状态继续。跟丢了，停下来，重述当前状态。

## 规则 11 — 遵循代码库惯例

在代码库内，遵从惯例胜过个人品味。

匹配现有的命名、文件结构、错误处理、格式和测试风格。如果某项惯例看起来有害，明确指出来，不要悄悄另立一套。

## 规则 12 — 响亮失败

"完成"如果静默跳过任何东西，就是错误陈述。

"测试通过"如果测试被跳过或没跑，就是错误陈述。

默认情况：暴露不确定性、缺失的检查、跳过的工作、未解决的冲突。

## 成功信号

这些准则生效的标志：diff 中不必要的改动减少，因过度复杂而重写的次数减少，歧义在落地前被摆出来，每一行改动都可追溯到用户的具体请求。

## 项目编码规则

### 技术栈

- 运行时：[Node/Python/Rust 等]
- 框架：[React/Next/Vite 等]
- 包管理器：[npm/pnpm/uv/cargo 等]

### 命令

- 安装：[命令]
- 开发：[命令]
- 测试：[命令]
- 类型检查：[命令]
- 静态检查：[命令]
- 构建：[命令]

### 验证

- 修改运行时行为后，运行 [测试命令]。
- 修改类型或公共接口后，运行 [类型检查命令]。
- 修改前端 UI 后，在 [本地 URL] 用浏览器验证。
- 若跳过某项检查，明确报告。

### 边界

- 未经明确要求，不编辑 [敏感路径]。
- 不手工修改生成的产物。
- 不修改迁移、锁文件、环境文件或生产配置，除非任务明确要求。
- 保留用户既有改动。

### 本地惯例

- 遵循现有模块布局（[src/...]）。
- 优先复用现有辅助函数，而不是新抽象。
- 贴合邻近测试风格。
- 改动范围放在请求之内。