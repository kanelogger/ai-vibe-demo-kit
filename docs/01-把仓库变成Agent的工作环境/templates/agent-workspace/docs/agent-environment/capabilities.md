# Agent capabilities

能力状态使用：`unknown`、`unavailable`、`installed`、`available`、`authenticated`、`authorized`、`healthy`、`blocked-by-policy`。只有 `healthy` 可直接用于任务。记录 provider/interface、操作、前置条件、探测方式、权限、限制、验证时间和证据来源。
