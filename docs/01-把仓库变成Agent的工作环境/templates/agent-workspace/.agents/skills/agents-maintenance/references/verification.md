# New-context verification

在严格模式写入和确定性检查通过后，使用新的会话或独立 Verifier 做只读验收。不要打开旧草稿或参考模板，只检查当前自动加载的规则。

验收至少确认：

- 仓库用途与项目级规则一致。
- 用户级内容是跨项目稳定偏好。
- 项目级内容是当前仓库约束。
- 最小验证命令和按需入口可解析。
- 没有占位符、模板说明、重复规则或无法消解的冲突。

若无法确认自动加载来源，标记为“未证明”；不得推断验证通过。
