interface:
  display_name: "Workflow Runner"
  short_description: "Run Harness stages with dynamic capability selection"
  default_prompt: "Use $workflow-runner to complete the current Harness workflow stage and submit evidence safely."
policy:
  allow_implicit_invocation: true
